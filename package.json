{
  "name": "my-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "^4.0.3",
    "sonar-scanner": "^3.1.0"
  },
  "scripts": {
    "start": "set NODE_OPTIONS=--openssl-legacy-provider && react-scripts start --env=jsdom",
    "build": "set NODE_OPTIONS=--openssl-legacy-provider && react-scripts build --env=jsdom",
    "test": "set NODE_OPTIONS=--openssl-legacy-provider && react-scripts test --env=jsdom",
    "eject": "set NODE_OPTIONS=--openssl-legacy-provider && react-scripts eject --env=jsdom"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
